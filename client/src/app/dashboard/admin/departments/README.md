# departments/ — Departments Management Page

Route: `/dashboard/admin/departments`

CRUD interface for managing academic departments. Calls `GET/POST/PUT/DELETE /api/departments`.

## Connections

- **Server module**: `server/src/modules/department/`
- **Shared UI**: `DataTable`, `FormModal`, `ConfirmDialog`, `Toast`
